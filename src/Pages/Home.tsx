import { useEffect } from "react";
import { GetPageTitle } from "../Utils/RouteNames";

function Home() {
  // useEffect(() => {
  //   document.title = GetPageTitle(window.location.pathname) + " | My Portfolio";
  // }, []);
  return <div>Home</div>;
}

export default Home;
